# Билет 20. Monte Carlo path tracing, importance sampling, MIS, MCMC и MLT

## Интеграл освещенности и path tracing

Физически корректный рендеринг сводит изображение к интегралам переноса света. В PBDR-конспекте пиксельная величина задается measurement equation:

$$
I_k=\int_A\int_{S^2} W_k(p,\omega)L_i(p,\omega)\,d\omega^\perp dp,
$$

где $I_k$ - значение пикселя, $W_k$ - importance-функция сенсора/пикселя, $L_i$ - входящий радианс, $p$ - точка на сенсоре, $\omega$ - направление. Перенос света также описывается уравнением рендеринга:

$$
L_o(x,\omega_o)=L_e(x,\omega_o)+
\int_{S^2}L_i(x,\omega_i)f_s(x,\omega_i,\omega_o)\,d\sigma(\omega_i).
$$

Здесь $L_o$ - исходящий радианс, $L_e$ - собственное излучение, $f_s$ - BSDF с косинусным множителем, $d\sigma$ - мера направлений. Path tracing оценивает это уравнение методом Монте-Карло: выпускает луч из камеры, строит случайный путь с отражениями/рассеяниями и суммирует вклад путей. В операторной форме решение раскладывается в ряд Неймана:

$$
L_o=(I-K_SK_T)^{-1}L_e
=\sum_{i=0}^{\infty}(K_SK_T)^iL_e,
$$

где члены ряда соответствуют путям с разным числом взаимодействий.

## Обычный Monte Carlo

Для интеграла

$$
I=\int_\Omega f(x)\,dx
$$

при выборке $X_i\sim p(x)$ используется оценка

$$
\hat I=\frac{1}{N}\sum_{i=1}^{N}\frac{f(X_i)}{p(X_i)}.
$$

Она несмещенная, если $p(x)>0$ там, где $f(x)\ne0$, а ошибка убывает как $O(1/\sqrt{N})$. Проблема в рендеринге - большая размерность пространства путей и высокая дисперсия: редкие, но важные пути, например каустики, могут почти не сэмплироваться.

## Importance Sampling и MIS

**Выборка по значимости** уменьшает дисперсию: сэмплы берутся чаще там, где вклад $|f(x)|$ велик. Идеально

$$
p(x)\propto |f(x)|.
$$

В рендеринге это означает выбор направлений по BSDF, выбор точек на источниках света, выбор важных пикселей или ребер разрыва в дифференцируемом рендеринге.

**Multiple Importance Sampling (MIS)** комбинирует несколько стратегий, потому что одна стратегия редко хороша во всех случаях. Оценка имеет вид

$$
\hat I=
\sum_j\frac{1}{n_j}\sum_{i=1}^{n_j}
w_j(X_{j,i})\frac{f(X_{j,i})}{p_j(X_{j,i})}.
$$

Вес $w_j$ увеличивает вклад стратегии, которая лучше подходит для данного сэмпла. Например, light sampling хорошо работает для прямого света, BSDF sampling - для отражений, edge sampling - для boundary term.

## MCMC и Metropolis Light Transport

В обычном Monte Carlo сэмплы независимы. **MCMC** строит цепочку зависимых сэмплов:

$$
X_1\to X_2\to\dots\to X_N.
$$

Новый сэмпл получается **мутацией** текущего: proposal kernel $T(x\to y)$ предлагает состояние $y$ по текущему состоянию $x$. В MLT состояние - световой путь, а мутация - малое изменение пути: вершины, направления, длины подцепочки или случайных параметров.

**Metropolis Light Transport (MLT)** использует MCMC в пространстве световых путей. Кандидат $y$ принимается с вероятностью

$$
a(x\to y)=
\min\left(1,\frac{F(y)T(y\to x)}{F(x)T(x\to y)}\right),
$$

где $F(x)$ - скалярная важность/вклад пути. Цель - получить стационарное распределение

$$
p(x)=\frac{F(x)}{b}, \qquad b=\int_{\mathcal P}F(x)\,dx.
$$

Если $F_j(x)$ - вклад пути $x$ в пиксель $j$, то

$$
I_j=\int_{\mathcal P}F_j(x)\,dx
=b\,\mathbb E_{x\sim p}\left[\frac{F_j(x)}{F(x)}\right]
\approx \frac{b}{N}\sum_{i=1}^N\frac{F_j(X_i)}{F(X_i)}.
$$

MLT полезен для редких важных путей: он может "застрять" в области большого вклада и исследовать ее мутациями. Недостатки - коррелированные сэмплы, сложная сходимость и хуже параллельность.
